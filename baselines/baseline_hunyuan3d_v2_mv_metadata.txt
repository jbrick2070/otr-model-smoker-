Model: HunyuanVideo 3D-DIT v2 (single-view, non-multiview variant)
Version: hunyuan3d-dit-v2-0
Official Source: https://docs.comfy.org/tutorials/3d/hunyuan3D-2
Extracted from: EXIF metadata embedded in hunyuan3d-non-multiview-train.webp (official example image)
Fetched: 2026-06-30
Node Count: 11
Link Count: 12
Last Node ID: 80
Last Link ID: 166
Workflow Type: Image-to-Mesh (single view, no texture/multiview)
Input Nodes: ["LoadImage"]
Output Nodes: ["SaveGLB"]
Key Nodes: ImageOnlyCheckpointLoader, Hunyuan3Dv2Conditioning, EmptyLatentHunyuan3Dv2, KSampler, VAEDecodeHunyuan3D, VoxelToMeshBasic
VRAM Estimate (RTX 5080): 6-8 GB (shape generation only, no texture stage)
Quality Notes: Note this is the non-multiview single-image variant per handoff spec; ComfyUI natively supports Hunyuan3D-2mv shape generation but not texture/material generation. Node count (11) is close to the ~5-6 node estimate in the spec once you account for stock ComfyUI's split conditioning/latent/sampler/decode nodes.
Known Limitations: Native ComfyUI support does not include texture/material baking (shape only).

CHECKPOINT MISMATCH — NOT AUTO-FIXED, NEEDS A DECISION (kibitz r2, Antigravity, verified against disk 2026-06-30): this baseline's ImageOnlyCheckpointLoader widget points at "hunyuan3d-dit-v2.safetensors" (the single-view, non-multiview checkpoint), but the ONLY Hunyuan3D checkpoint actually on disk is "hunyuan3d-dit-v2-mv.safetensors" (the MULTIVIEW-trained variant). Unlike the FLUX/LTX filename fixes, this is not a safe blind rename: the -mv checkpoint was trained expecting the Hunyuan3Dv2ConditioningMultiView node (multiple view images), not this baseline's single-view Hunyuan3Dv2Conditioning node. Loading the -mv weights into the non-mv graph may run without erroring but could produce degraded output since the checkpoint doesn't match its trained conditioning path. Two real options: (a) keep this single-view JSON as the "official minimal non-mv" reference per the original handoff spec, but note it needs the non-mv checkpoint downloaded separately to run correctly, or (b) rebuild this baseline from the actual Hunyuan3D-2mv multiview example workflow (also documented at the same docs.comfy.org page) to match what's installed. Not resolved automatically — flagging for a decision before Phase 3 load-testing.
Verified: NO — pending load test in ComfyUI (Phase 3, next window)
